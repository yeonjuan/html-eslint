coverage:
  status:
    patch:
      default:
        target: 90%
    project:
      default:
        target: auto
        threshold: 2%
comment:
  layout: "diff, flags, files"
  behavior: new
  require_changes: false
